# Discord API Wrapper
discord.py

# Asynchronous MongoDB Driver
motor

# MongoDB low-level driver
pymongo

# Image Processing for UI/Embeds (if needed for dynamic banners)
Pillow

# Asynchronous operations
asyncio

# Environment variable management
python-dotenv
